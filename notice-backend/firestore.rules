rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the authenticated HTTPS backend uses this separate database.
    match /{document=**} { allow read, write: if false; }
  }
}
